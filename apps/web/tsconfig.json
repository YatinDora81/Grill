{
  "extends": "@repo/typescript-config/nextjs.json",
  "compilerOptions": {
    "plugins": [
      {
        "name": "next"
      }
    ],
    "strictNullChecks": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "next-env.d.ts",
    "next.config.js",
    ".next/types/**/*.ts"
  ],
  "//": "Test files are excluded from the build typecheck: they are not shipped, and bun:test's runner transpiles without typechecking, so holding fixtures to the app's strict config buys nothing the test run doesn't already cover. `bun run test` is their check.",
  "exclude": ["node_modules", "**/*.test.ts", "**/*.test.tsx", "test/**"]
}
