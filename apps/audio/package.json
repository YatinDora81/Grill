{
  "name": "@repo/audio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt",
    "dev": ".venv/bin/uvicorn main:app --reload --port 8000",
    "start": ".venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "test": ".venv/bin/pytest -q"
  }
}
